# Example local API configuration.
# Do not commit real credentials in .env.
# Replace the connection string when reusing another PostgreSQL setup.

HTTP_ADDRESS=:8080
DATABASE_URL=postgres://postgres:postgres@localhost:5432/household_access?sslmode=disable
